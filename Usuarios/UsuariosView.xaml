<UserControl x:Class="Sistema_de_Gestión_Farmacéutica.UsuariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sistema_de_Gestión_Farmacéutica"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnAgregar" Content="➕ Agregar Usuario" Width="150" Margin="5" Click="btnAgregar_Click"/>
            <Button x:Name="btnEliminar" Content="🗑 Eliminar Usuario" Width="150" Margin="5" Click="btnEliminar_Click"/>
            <Button x:Name="btnEditar" Content="⚙️ Editar Usuario" Width="150" Margin="5" Click="btnEditar_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgUsuarios" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id_usuario}" Width="Auto"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding nombre}" Width="*"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding apellido}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="2*"/>
                <DataGridTextColumn Header="Rol" Binding="{Binding rol}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>