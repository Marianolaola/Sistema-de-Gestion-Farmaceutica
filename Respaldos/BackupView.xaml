<UserControl x:Class="Sistema_de_Gestión_Farmacéutica.Respaldos.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sistema_de_Gestión_Farmacéutica.Respaldos"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Backup de Base de Datos"
                   FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Left" Margin="0,0,0,25"
        />
        <GroupBox Header="Directorio de Destino" Grid.Row="1" Margin="0,0,0,10" HorizontalAlignment="Left" 
                  Width="Auto">
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="txtRutaBackup" Grid.Row="0" 
                     FontSize="14" VerticalContentAlignment="Center" Padding="5" Margin="10" Width="300" Height="35"/>
                <Button x:Name="btnRuta" Grid.Column="1" Content="Examinar..." Width="120" Height="35" Margin="0,10,10,10" 
                    VerticalAlignment="Center" Click="btnRuta_Click" />
            </Grid>
        </GroupBox>
            

        <Button Grid.Row="2" x:Name="btnCrearBackup" Content="💾 Crear Backup" FontSize="16"
                FontWeight="SemiBold" Background="#007ACC" Foreground="White" Width="200" Height="45"
                HorizontalAlignment="Left" Cursor="Hand" Click="btnCrearBackup_Click" />
    </Grid>
</UserControl>
