<UserControl x:Class="Sistema_de_Gestión_Farmacéutica.Ventas.VentasDetalleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sistema_de_Gestión_Farmacéutica.Ventas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nueva Venta" FontSize="20" Margin="0,0,0,10" />

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>

            <!-- Cliente -->
            <StackPanel Orientation="Vertical" Margin="0,0,0,10" Grid.ColumnSpan="1">
                <TextBlock Text="Cliente"  Margin="0,0,5,0"/>
                <ComboBox x:Name="cmbClientes" HorizontalAlignment="Left" Width="200" DisplayMemberPath="nombre" SelectedValuePath="id_cliente"/>
                <!--Button x:Name="btnAgregar" Content="➕ Agregar Venta" Width="150" Margin="5" /-->
            </StackPanel>

            <!-- Seleccion de medicamento -->
            <StackPanel Orientation="Vertical" Margin="0,0,0,10" Grid.Row="1" Grid.ColumnSpan="4">
                <TextBlock Text="Medicamento" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <StackPanel Orientation="Horizontal">
                    <ComboBox x:Name="cmbMedicamentos" Width="200" DisplayMemberPath="nombre_comercial" SelectedValuePath="id_medicamento"/>
                    <TextBlock Text="Cantidad:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <TextBox x:Name="txtCantidad" Width="50"/>
                    <Button x:Name="btnAgregar" Content="➕ Sumar Producto " Margin="10,0,0,0" Click="btnAgregar_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Datagrid -->
        <DataGrid x:Name="dgDetalle" Grid.Row="2" AutoGenerateColumns="False" Margin="0,10,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Medicamento" Binding="{Binding Nombre}" Width="Auto"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="Auto"/>
                <DataGridTextColumn Header="Precio Unitario" Binding="{Binding Precio}" Width="Auto"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Total y Botones -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <TextBlock Text="Total: $" VerticalAlignment="Center" FontSize="16"/>
            <TextBlock x:Name="txtTotal" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Margin="5,0,20,0"/>
            <Button x:Name="btnConfirmar" Content="Confirmar Venta" Click="btnConfirmar_Click"  Width="150" />
        </StackPanel>



        <!--DataGrid x:Name="dgVentasDetalle" Grid.Row="2" AutoGenerateColumns="True" IsReadOnly="True" SelectionMode="Single" Margin="0,10,0,0"/-->
    </Grid>
</UserControl>
