<UserControl x:Class="Sistema_de_Gestión_Farmacéutica.Ventas.VentasDetalleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="850" Background="{StaticResource ColorVerdeFondo}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Margin="10,0,10,10" VerticalAlignment="Top">
            <GroupBox.Header>
                <TextBlock Text="Cliente" FontSize="16" Foreground="{StaticResource ColorBlancoRotoFondo}"/>
            </GroupBox.Header>

            <Grid Margin="10,5,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Label Cliente -->
                    <ColumnDefinition Width="200"/>
                    <!-- ComboBox Cliente -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Label Nombre -->
                    <ColumnDefinition Width="200"/>
                    <!-- TextBox Nombre -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Label DNI -->
                    <ColumnDefinition Width="200"/>
                    <!-- TextBox DNI -->
                </Grid.ColumnDefinitions>

                <!-- Cliente -->
                <TextBlock Text="Cliente:" 
                   VerticalAlignment="Center" 
                   Grid.Column="0" 
                   Margin="0,0,5,0"/>

                <ComboBox x:Name="cmbClientes"
                  Width="200"
                  DisplayMemberPath="nombre"
                  SelectedValuePath="id_cliente"
                  SelectionChanged="cmbClientes_SelectionChanged"
                  Grid.Column="1"/>

                <!-- Nombre -->
                <TextBlock Text="Nombre:" 
                   VerticalAlignment="Center" 
                   Margin="15,0,5,0" 
                   Grid.Column="2"/>

                <TextBox x:Name="txtNombreCliente"
                 Width="200"
                 Grid.Column="2" Grid.ColumnSpan="2" Margin="62,8,5,8"/>

                <!-- DNI -->
                <TextBlock Text="DNI:" 
                   VerticalAlignment="Center" 
                   Margin="15,0,5,0" 
                   Grid.Column="4"/>

                <TextBox x:Name="txtDniCliente"
                 Width="200"
                 Grid.Column="4" Grid.ColumnSpan="2" Margin="43,8,0,8"/>
            </Grid>
        </GroupBox>

        <!-- Medicamentos -->
        <Grid Margin="0,0,0,10" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Grid.Row="0" Margin="10,0,10,0">
                <GroupBox.Header>
                    <TextBlock Text="Producto" FontSize="16" Foreground="{StaticResource ColorBlancoRotoFondo}"/>
                </GroupBox.Header>

                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
                        <ComboBox x:Name="cmbMedicamentos" Width="200" 
                                  DisplayMemberPath="nombre_comercial" 
                                  SelectedValuePath="id_medicamento"
                                  SelectionChanged="cmbMedicamentos_SelectionChanged"/>

                        <TextBlock Text="Nombre: " VerticalAlignment="Center" Margin="10,0,5,0"/>
                        <TextBox x:Name="txtNombreMedicamento" Width="150" IsReadOnly="True"/>

                        <TextBlock Text="Descripción: " VerticalAlignment="Center" Margin="10,0,5,0"/>
                        <TextBox x:Name="txtDescripcionMedicamento" Width="200" IsReadOnly="True"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="1">
                        <TextBlock Text="Precio Unitario: " VerticalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBox x:Name="txtPrecio" Width="80" IsReadOnly="True"/>

                        <TextBlock Text="Cantidad: " VerticalAlignment="Center" Margin="10,0,5,0"/>
                        <TextBox x:Name="txtCantidad" Width="50"/>

                        <TextBlock Text="Stock disponible: " VerticalAlignment="Center" Margin="10,0,5,0"/>
                        <TextBox x:Name="txtStock" Width="50" IsReadOnly="True" />
                    </StackPanel>
                </Grid>
            </GroupBox>

            <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Vertical" VerticalAlignment="Center">
                <Button x:Name="btnAgregar" 
                        Content="➕ Agregar" 
                        Width="120"
                        Height="35"
                        Margin="0,0,0,10"
                        FontWeight="Semibold"
                        Click="btnAgregar_Click"/>

                <Button x:Name="btnLimpiar"
                        Content="❌ Limpiar"
                        Width="120"
                        Height="35"
                        FontWeight="Semibold"
                        Click="btnLimpiar_Click"/>
            </StackPanel>

            <Border Grid.Row="1" Grid.ColumnSpan="2" Margin="0,10,0,10" Background="{StaticResource ColorBlancoRotoFondo}" CornerRadius="5" Padding="10">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="5"/>
                </Border.Effect>

                <DataGrid x:Name="dgDetalle" 
                      AutoGenerateColumns="False"  
                      IsReadOnly="True" 
                      CanUserAddRows="False"
                      Background="Transparent" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Medicamento" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Presentación" Binding="{Binding Presentacion}" Width="*"/>
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="Auto"/>
                        <DataGridTextColumn Header="Precio Unitario" Binding="{Binding Precio}" Width="Auto"/>
                        <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}" Width="Auto"/>
                        <DataGridTemplateColumn Header="Quitar" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="❌ Quitar" Click="btnQuitar_Click" Background="LightCoral" 
                                        Foreground="White" Padding="4,2" BorderThickness="0" Cursor="Hand" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Total y botones -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Total: $" VerticalAlignment="Center" FontSize="16"/>
            <TextBlock x:Name="txtTotal" 
               VerticalAlignment="Center" 
               FontSize="16" 
               FontWeight="Bold" 
               Margin="5,0,20,0"/>
            
            <Button x:Name="btnConfirmar" 
                    Content="Confirmar Venta" 
                    Width="150"
                    Click="btnConfirmar_Click"
                    Style="{StaticResource SmallPrimaryActionButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
