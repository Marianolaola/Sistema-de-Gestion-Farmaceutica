<UserControl x:Class="Sistema_de_Gestión_Farmacéutica.Ventas.MisVentas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sistema_de_Gestión_Farmacéutica.Ventas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="0,0,0,10" Grid.Row="0" Grid.ColumnSpan="2">
            <TextBlock x:Name="Titulo" Text="Mis Ventas" FontSize="24" Margin="10"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="1">
            <Button x:Name="btnAgregar" Content="Imprimir Venta" Click="btn_ImprimirClick" Width="150" Margin="5" />
        </StackPanel>

        <DataGrid x:Name="dgVentas" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Venta" Binding="{Binding id_venta}" Width="*"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding fecha_venta}" Width="*"/>
                <DataGridTextColumn Header="ID Cliente" Binding="{Binding id_cliente}" Width="*"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding nombre_completo}" Width="*"/>
                <DataGridTextColumn Header="Total" Binding="{Binding total}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
